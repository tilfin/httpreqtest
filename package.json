{
  "name": "httpreqtest",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "mocha": "1.14.*",
    "should": "2.1.*",
    "supertest": "0.8.*",
    "istanbul" : "0.1.45",
    "coveralls": "~2.5.0",
    "mocha-lcov-reporter": "0.0.1"
  },
  "scripts": {
    "test": "./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec test/spec test/",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js; rm -fr coverage/"
  }
}
